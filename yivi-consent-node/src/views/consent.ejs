<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Consent request</title>
</head>
<body>
    <h1>An application requests access to your data!</h1>
    <form action="/consent" method="post">
        <input type="hidden" name="challenge" value="<%= challenge %>">
        <% if (client.logo_uri) { %>
         <img width="400px" src="<%= client.logo_uri %>" alt="client logo">
        <% } %>
        <p>Hi <%= user %>, <strong><%= client.client_name || client.client_id %></strong> wants to access resources on your behalf and to:</p>
            <ul>

            
        <% requested_scope.forEach(scope => { %>
            <li><%= scope %></li>
        <% }) %>
            </ul>

        <p><input type="checkbox" name="remember" id="remember" value="1"><label for="remember">Do not ask me again</label></p>
    
        <p>
            <input type="submit" name="submit" id="accept" value="Allow access">
            <input type="submit" name="submit" id="reject" value="Deny access">
        </p>

    </form>
</body>
</html>